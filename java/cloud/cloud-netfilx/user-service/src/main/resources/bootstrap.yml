server:
  port: 3355

spring:
  application:
    name: user-service
  cloud:
    config:
      label: master
      profile: dev
      uri: http://127.0.0.1:3344

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka